export interface ReportState {
    context?: any,
    registerData?: any,
    error?: Error,
    error_register?: Error,
    formId?: string,
    docId?: string,
    formData?: any,
    tabNo?: string,
    userDetails?: any
}